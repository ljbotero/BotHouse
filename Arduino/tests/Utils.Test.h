#include "../src/Config.h"
#ifdef RUN_UNIT_TESTS
#include "../src/Utils.h"

test(Mesh_startsWith, ADoesStartWithB) {
  bool actual = true;
  bool expected = true;
  assertEqual(actual, expected);
}

#endif
